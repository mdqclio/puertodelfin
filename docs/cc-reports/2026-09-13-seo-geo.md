# Pasada SEO + GEO — index.html

**Fecha:** 2026-09-14 · **Archivos:** `index.html`, `sitemap.xml`
**Restricciones respetadas:** sin tocar fotos ni layout de secciones, sin JS nuevo, sin requests nuevos (+~7 KB de HTML, sin impacto en perf). Sin `aggregateRating` ni `Review` en JSON-LD. Sin datos inventados.

---

## 1. Reputación
- Airbnb **4,94 → 4,87** en los 3 lugares donde aparecía: franja de números, KPI de opiniones y texto legal (`rev-disclaimer`).
- Cantidad de evaluaciones Airbnb: **16**, sin cambios.
- Google 4,5 / 406: sin cambios.

## 2. JSON-LD `LodgingBusiness`
- `numberOfRooms`: 3 → **12**.
- `containsPlace` con 3 `Accommodation`:
  - Refugio de Mar — occupancy 2–3 — 38 m² — "Monoambiente. Unidades en planta baja y en planta alta con techo a dos aguas."
  - Cabaña de Mar — occupancy hasta 4 — 38 m² — "Dormitorio separado, planta baja, cerca de la pileta."
  - Casa de Mar — occupancy hasta 6 — 53 m² — "Dos pisos, en primer piso, la más amplia."
  - `floorSize` como `QuantitativeValue` con `unitCode: "MTK"` (m²).
- `amenityFeature`: se conservan Pileta, Parrilla, Hogar a leña, Admite mascotas, Calefacción. "Cocina equipada" → **"Cocina con horno y 4 hornallas"**. Sumados: **Wi-Fi, Microondas, TV 32'', Estacionamiento** (todos `value: true`).
- `sameAs`: solo `https://api.whatsapp.com/send?phone=5492235365385` (única URL externa de perfil/contacto presente en el HTML).
- Ambos bloques JSON-LD validados como JSON (parse OK).

### URLs `sameAs` que FALTAN (no están en el HTML, no las inventé)
Pasámelas y las sumo:
1. **Instagram** — no hay ningún link a Instagram en el sitio.
2. **Facebook** — ídem.
3. **Airbnb** — URL del anuncio/perfil (el sitio cita 4,87★ y Superanfitrión, pero no linkea).
4. **Google Business Profile / Google Maps** — URL pública del perfil (el iframe del mapa tiene el place ID `0x959b5c2d52a1b1a5:0x8aa14c4ea5199bba`, pero no construí una URL a partir de él).
5. **TripAdvisor** — si existe ficha.
6. **Booking / otras OTAs** — si existen (el sitio dice "sin Booking", así que probablemente no aplique).

Nota: el `sameAs` de WhatsApp es débil semánticamente (schema.org espera páginas de perfil). Vale la pena reemplazarlo/complementarlo con Instagram y Google cuando me pases las URLs.

## 3. Copy factual visible
- Línea nueva en el header de Cabañas (`.cab-fact`): **"12 cabañas en 3 tipologías, para 2 a 6 personas."**
- Bloque único de equipamiento al pie de la grilla de cabañas (`.cab-equip`, una sola vez):
  > **Todas con parrilla, hogar a leña, cocina con horno y 4 hornallas, microondas y TV 32''.** Wi-Fi en todas las cabañas.
  > Mascotas bienvenidas, de cualquier tamaño y sin costo extra.
- Fichas: Refugio ya decía "38 m² monoambiente" (se mantiene); Cabaña ya decía "Dormitorio separado" (se mantiene); Casa: spec "2 plantas" → **"Dos pisos"** y en la descripción "en dos plantas" → "en dos pisos". m² sin cambios (38 / 38 / 53).
- Mascotas "de cualquier tamaño y sin costo extra": visible en el bloque de equipamiento y en la FAQ.

## 4. FAQ
- Sección nueva `#preguntas` ("Preguntas frecuentes") entre Opiniones y Ubicación. Acordeón nativo `<details>/<summary>` (sin JS), serif Cormorant en preguntas, paleta de marca, fondo `--c`.
- 9 preguntas, con schema `FAQPage` en un segundo `<script type="application/ld+json">`; el texto del schema coincide con el visible.
- "¿Cómo reservo?" usa la promesa que ya estaba en el sitio: **"Respondemos en menos de 1 hora"** (hero, mid-CTA, CTA final).
- "¿Horarios?" toma los del JSON-LD: check-in 14:00 / check-out 10:00.
- Nav: **"Preguntas"** agregado en nav principal y footer. Para que 6 ítems entren entre 900 y 1100 px, gap del nav baja de 32 a 22 px en ese rango (media query nueva). Verificado en headless: una sola fila, sin overflow, a 905 / 1024 / 1280 / 1440 px. Móvil: menú vertical, entra bien.
- `scroll-margin-top` sumado a `#preguntas`.

## 5. H1
- `<h1>Cabañas en Mar de las Pampas, a 50 m del mar</h1>` (con `&nbsp;` en "50 m" para que no parta la línea en móvil).
- "El lujo de / no hacer nada." pasa a `<p class="hero-claim">` inmediatamente debajo, con **exactamente el mismo estilo** (Cormorant 300, mismo clamp de tamaño, misma altura de línea).
- Ajustes para que el bloque más alto siga entrando en el hero sin romper nada:
  - `.hero-body` max-width 720 → 940 px, así el H1 ocupa 2 líneas a 84 px (antes quedaban 3 y el texto chocaba con el nav en pantallas de 720 px de alto). Medido en headless: H1 2 líneas + claim 2 líneas en 905–1440 px; 3 + 2 en 390 px. Tope del bloque a 129 px en 1280×720 (nav mide 73 px): no se pisa.
  - Gradiente del hero: parada intermedia 0.25 → 0.38 (y 0.08 → 0.10 arriba) para legibilidad del H1, que ahora queda más arriba, sobre cielo y pared blanca. No se tocó la foto.
- Los H2 quedan igual. Hay un solo `<h1>` en la página (verificado).

## 6. Sitemap
- `lastmod` 2026-05-31 → **2026-09-14**.

---

## Verificación
- Render headless (Chromium vía Playwright) a 1440×900, 1280×720, 1024×768, 905×700 y 390×844: sin scroll horizontal, nav en una fila en desktop, FAQ abre/cierra, bloque de equipamiento visible al pie de cabañas.
- JSON-LD: ambos bloques parsean como JSON válido. Conviene pasar la URL por https://validator.schema.org/ una vez deployado.

## Pendientes para Leonardo
- URLs para `sameAs` (ver lista arriba).
- Si cambió la cantidad de evaluaciones de Airbnb (hoy 16), decime la cifra.
